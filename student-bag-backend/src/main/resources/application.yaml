server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_bag
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:ayman@123}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:ayman.aljamal2017@gmail.com}
    password: ${MAIL_PASSWORD:ihep rqnx jyyl ucnp}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: ${JWT_SECRET:your_super_secret_key_123456789012345678901234567890}
    expiration: 86400000

openai:
  api-key: ${OPENAI_API_KEY}
  model: ${OPENAI_MODEL:gpt-4.1-mini}
  base-url: https://api.openai.com/v1